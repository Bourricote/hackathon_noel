<nav id="nav-bar" class="nav-extended">
    <div class="container justify-content-center nav-content py-3">
        <ul class="tabs d-flex justify-content-center tabs-transparent">
            <li class="tab typo_title"><a href="{{ path('default') }}">Home</a></li>
            <li class="tab typo_title"><a class="active" href="{{ path('mission_index') }}">Missions</a></li>
            <li><a class="active" href="#missions"><img class="resize" src="{{asset('build/images/logo.png')}}" alt=""></a></li>
            {% if app.user %}
            <li class="tab disabled typo_title"><a href="{{ path('user_show', {'id' : app.user.id }) }}">Profil</a></li>
            {% else %}
            <li class="tab disabled typo_title"><a href="{{ path('app_register') }}">Sign up</a></li>
            {% endif %}
            {% if app.user %}
            <li class="tab typo_title"><a href="{{ path('app_logout') }}">Logout</a></li>
            {% else %}
            <li class="tab typo_title"><a href="{{ path('app_login') }}">Login</a></li>
            {% endif %}
        </ul>
    </div>
</nav>



